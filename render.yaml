services:
  - type: web
    name: soukenligne
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: SESSION_SECRET
        generateValue: true
    disk:
      name: soukenligne-data
      mountPath: /opt/render/project/src/data
      sizeGB: 1
