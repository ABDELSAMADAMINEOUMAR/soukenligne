<%- include('partials/header') %>

<section class="section product-page">
  <div class="container">
    <!-- Breadcrumb -->
    <nav class="breadcrumb">
      <a href="/">Accueil</a>
      <span>›</span>
      <a href="/boutique">Boutique</a>
      <% if (product.category_name) { %>
      <span>›</span>
      <a href="/boutique?categorie=<%= product.category_slug %>"><%= product.category_name %></a>
      <% } %>
      <span>›</span>
      <span><%= product.name %></span>
    </nav>

    <div class="product-detail">
      <!-- Images -->
      <div class="product-gallery">
        <div class="product-main-image" id="main-image-container">
          <% if (images.length > 0) { %>
          <img src="<%= images[0].image_path %>" alt="<%= product.name %>" id="main-product-image">
          <% } else { %>
          <div class="product-card-placeholder" style="height:400px;display:flex;align-items:center;justify-content:center;background:var(--gray-100);border-radius:12px">
            <svg width="80" height="80" viewBox="0 0 24 24" fill="none" stroke="#ccc" stroke-width="1"><rect x="3" y="3" width="18" height="18" rx="2"/><circle cx="8.5" cy="8.5" r="1.5"/><polyline points="21 15 16 10 5 21"/></svg>
          </div>
          <% } %>
        </div>
        <% if (images.length > 1) { %>
        <div class="product-thumbs">
          <% images.forEach((img, i) => { %>
          <button class="product-thumb <%= i === 0 ? 'active' : '' %>" onclick="changeImage(this, '<%= img.image_path %>')">
            <img src="<%= img.image_path %>" alt="<%= product.name %> image <%= i + 1 %>">
          </button>
          <% }); %>
        </div>
        <% } %>
      </div>

      <!-- Info -->
      <div class="product-info">
        <% if (product.brand) { %>
        <span class="product-brand"><%= product.brand %></span>
        <% } %>

        <h1 class="product-title"><%= product.name %></h1>

        <div class="product-pricing">
          <% if (product.discount_price) { %>
          <span class="product-price-current"><%= formatPrice(product.discount_price) %></span>
          <span class="product-price-original"><%= formatPrice(product.price) %></span>
          <span class="product-discount">-<%= Math.round((1 - product.discount_price / product.price) * 100) %>%</span>
          <% } else { %>
          <span class="product-price-current"><%= formatPrice(product.price) %></span>
          <% } %>
        </div>

        <div class="product-availability">
          <% if (product.is_available && product.stock_quantity > 0) { %>
          <span class="avail-badge avail-in">✓ En stock</span>
          <% if (product.stock_quantity <= 5) { %>
          <span class="avail-low">Plus que <%= product.stock_quantity %> en stock !</span>
          <% } %>
          <% } else { %>
          <span class="avail-badge avail-out">Rupture de stock</span>
          <% } %>
        </div>

        <% if (product.short_description) { %>
        <p class="product-short-desc"><%= product.short_description %></p>
        <% } %>

        <% if (typeof currentUser !== 'undefined' && currentUser && currentUser.role === 'admin') { %>
        <div class="product-actions" style="margin-top: 1.5rem;">
          <a href="/admin/produits/modifier/<%= product.id %>" class="btn btn-outline btn-lg btn-block" style="border-color: var(--primary); color: var(--primary);">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" style="vertical-align: middle; margin-right: 0.5rem;"><path d="M12 20h9"/><path d="M16.5 3.5a2.121 2.121 0 0 1 3 3L7 19l-4 1 1-4L16.5 3.5z"/></svg>
            Modifier ce produit
          </a>
        </div>
        <% } else { %>
          <% if (product.is_available && product.stock_quantity > 0) { %>
          <form action="/panier/ajouter" method="POST" class="product-actions">
            <input type="hidden" name="product_id" value="<%= product.id %>">
            <div class="quantity-selector">
              <button type="button" class="qty-btn" onclick="changeQty(-1)">−</button>
              <input type="number" name="quantity" value="1" min="1" max="<%= product.stock_quantity %>" id="qty-input" class="qty-input">
              <button type="button" class="qty-btn" onclick="changeQty(1)">+</button>
            </div>
            <button type="submit" class="btn btn-primary btn-lg btn-block add-to-cart-btn">
              <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="9" cy="21" r="1"/><circle cx="20" cy="21" r="1"/><path d="M1 1h4l2.68 13.39a2 2 0 0 0 2 1.61h9.72a2 2 0 0 0 2-1.61L23 6H6"/></svg>
              Ajouter au panier
            </button>
          </form>
          <% } %>

          <%
            const waPrice = product.discount_price || product.price;
            const waMsg = `Bonjour, je suis intéressé(e) par ce produit :\n\nProduit : ${product.name}\nPrix : ${formatPrice(waPrice)}\nQuantité : 1\n\nJe voudrais avoir plus d'informations et connaître les conditions de livraison.`;
          %>
          <a href="<%= whatsappLink(whatsappNumber, waMsg) %>" target="_blank" class="btn btn-wa btn-lg btn-block">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor"><path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347z"/><path d="M12 0C5.373 0 0 5.373 0 12c0 2.625.846 5.059 2.284 7.034L.789 23.492a.5.5 0 00.612.616l4.584-1.453A11.949 11.949 0 0012 24c6.627 0 12-5.373 12-12S18.627 0 12 0z"/></svg>
            Commander sur WhatsApp
          </a>
        <% } %>

        <div class="product-meta-info">
          <div class="meta-row">
            <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M5 12h14"/><path d="M12 5l7 7-7 7"/></svg>
            <span>Livraison disponible partout au Tchad</span>
          </div>
          <div class="meta-row">
            <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><rect x="1" y="4" width="22" height="16" rx="2"/><line x1="1" y1="10" x2="23" y2="10"/></svg>
            <span>Paiement à la livraison</span>
          </div>
        </div>
      </div>
    </div>

    <!-- Description -->
    <% if (product.description) { %>
    <div class="product-description">
      <h2>Description</h2>
      <div class="desc-content"><%= product.description %></div>
    </div>
    <% } %>

    <!-- Related products -->
    <% if (related.length > 0) { %>
    <div class="related-products">
      <h2 class="section-title">Produits similaires</h2>
      <div class="products-grid">
        <% related.forEach(product => { %>
          <%- include('partials/product-card', { product }) %>
        <% }); %>
      </div>
    </div>
    <% } %>
  </div>
</section>

<script>
function changeImage(thumb, src) {
  document.getElementById('main-product-image').src = src;
  document.querySelectorAll('.product-thumb').forEach(t => t.classList.remove('active'));
  thumb.classList.add('active');
}
function changeQty(delta) {
  const input = document.getElementById('qty-input');
  const val = parseInt(input.value) + delta;
  if (val >= 1 && val <= parseInt(input.max)) input.value = val;
}
</script>

<%- include('partials/footer') %>
